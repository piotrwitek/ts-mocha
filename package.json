{
  "name": "ts-mocha",
  "version": "1.0.3",
  "description": "Mocha thin wrapper that allows running TypeScript tests with TypeScript runtime (ts-node) to get rid of compilation complexity",
  "repository": "https://github.com/piotrwitek/ts-mocha",
  "author": "Piotr Witek <piotrek.witek@gmail.com> (http://piotrwitek.github.io)",
  "license": "MIT",
  "main": "src/index.js",
  "bin": {
    "ts-mocha": "./bin/ts-mocha"
  },
  "engines": {
    "node": ">= 0.10.x"
  },
  "scripts": {
    "clean": "rm -rf node_modules/",
    "reinstall": "yarn run clean && yarn install",
    "test:cli": "./bin/ts-mocha -p test test/**/*.spec.ts",
    "test:programmatic": "node test/programmatic-use-test.js",
    "test": "npm run test:cli & npm run test:programmatic",
    "prepublishOnly": "yarn run clean && yarn install --production && yarn add -D mocha && yarn test"
  },
  "dependencies": {
    "ts-node": "^3.3.0"
  },
  "devDependencies": {
    "@types/expect": "1.20.2",
    "@types/mocha": "2.2.44",
    "@types/node": "8.0.53",
    "expect": "22.0.0",
    "husky": "0.14.3",
    "mocha": "4.0.1",
    "tslint": "5.8.0",
    "typescript": "2.6.1"
  },
  "peerDependencies": {
    "mocha": "^3.X.X || ^4.X.X"
  },
  "files": [
    "bin/",
    "src/",
    "package.json",
    "README.md",
    "LICENSE",
    "yarn.lock"
  ],
  "keywords": [
    "ts",
    "mocha",
    "typescript",
    "ts-node",
    "test"
  ]
}
